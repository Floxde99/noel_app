name: Deploy to VPS (Zero Downtime)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY-Production }}

      - name: Deploy on VPS
        run: |
          ssh -o StrictHostKeyChecking=no \
          ${{ secrets.VPS_USER-Production }}@${{ secrets.VPS_HOST-Production }} << 'EOF'

          set -e
          cd ${{ secrets.VPS_PATH-Production }}

          git pull origin main
          npm ci

          npx prisma generate
          npx prisma migrate deploy

          npm run build

          pm2 reload ecosystem.config.js

          EOF
